# Environment Scan 保持本地观察

Environment Scan 只观察当前 App 运行用户可见的本地状态，不发起网络请求验证远程认证、查询在线版本或同步数据，以保持扫描时长可预期、离线可用并避免隐式暴露凭据。本地存在的远程认证配置只能记录为“已配置”，不能表述为“已登录”；如需联网验证，应由未来的独立显式操作触发。Environment Scan 可以记录凭据来源是否存在及其非敏感类型，但不读取、展示或持久化 token、密码、私钥或自定义凭据命令正文。
